<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.delivery.mapper.UserMapper">
	<insert id="registerUser" parameterType="com.springboot.delivery.model.User">
		insert into b_user_info(user_id,user_name,email,password,user_phone,point,birth) values(#{user_id},#{user_name},#{email},#{password},#{user_phone},0,to_date(#{birth},'YYYY-MM-DD'))
	</insert>
	<select id="loginUser" resultType="com.springboot.delivery.model.LoginUser" parameterType="com.springboot.delivery.model.LoginUser">
		select user_id, password, user_name from b_user_info where user_id=#{user_id} and password=#{password}
	</select>
	
	<select id="idcheck" parameterType="String" resultType="Integer">
	
	select count(*) from b_user_info where user_id=#{user_id}
	
	</select>
</mapper>